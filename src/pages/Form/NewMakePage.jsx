import { observer } from "mobx-react";
import AddNewMakeModul from "../../components/Form/AddNewMake";

const NewMakePage = () => {
  return <AddNewMakeModul></AddNewMakeModul>;
};

export default observer(NewMakePage);
